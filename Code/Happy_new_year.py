import time # для задержек
import sys  # для вывода без перевода строки

# Функция для вывода текста с эффектом печати
def slow_print(text, delay=0.04):
    for char in text:
        sys.stdout.write(char)
        sys.stdout.flush()
        time.sleep(delay)
    print()  # переводим строку в конце

# Запуск
slow_print("Инициализация новогоднего протокола...")
time.sleep(1)

slow_print("3...")
time.sleep(0.5)
slow_print("2...")
time.sleep(0.5)
slow_print("1...")
time.sleep(0.5)

slow_print("🎉 С НОВЫМ 2026 ГОДОМ! 🎉", delay=0.08)
time.sleep(0.7)

slow_print("Пусть каждый следующий коммит...")
slow_print("...будет хоть на байт легче предыдущего.", delay=0.06)
slow_print("А сегодня — просто дыши.", delay=0.1)

print("\n[Протокол завершён]")
print("\nP.S. Даже если этот скрипт никогда не запустят...")
print("Он уже здесь. Как и ты.")
